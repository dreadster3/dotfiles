.DS_Store

result

configurations/tmux/plugins
configurations/lazygit/state.yml
/configurations/hypr/.luarc.json
/configurations/nvim/.pi

# Secrets — defense in depth (sops keys live outside the repo, but block accidental commit)
*.age.key
id_*
!id_*.pub
